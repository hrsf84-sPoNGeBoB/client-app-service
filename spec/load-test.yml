config:
  target: 'http://client-app.client.d9d988bd.svc.dockerapp.io'
  phases:
    - duration: 2
      arrivalRate: 100
scenarios:
  - flow:
    - get:
        url: '/results?search_query=new+year'
